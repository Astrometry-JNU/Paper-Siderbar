# Paper Sidebar · 论文阅读助手

## 安装到 Chrome

1. 如果下载的是 ZIP，先解压，保留整个 paper-sidebar 文件夹。
2. 在 Chrome 地址栏输入 `chrome://extensions`，打开右上角「开发者模式」。
3. 点击「加载已解压的扩展程序」，选择包含 manifest.json 的 paper-sidebar 文件夹。
4. 在工具栏的拼图图标中找到「Paper Sidebar · 论文阅读助手」，将它固定。
5. 打开期刊论文的 HTML 网页，点击工具栏中的扩展图标，即可打开侧边栏。

需要 Chrome 116 或更新版本。文件夹是扩展的运行位置，安装后请勿删除或移动。此版本针对 Chrome；没有进行 Edge 验证。

## 配置与阅读

打开「设置」，选择服务商，填写 API Key 和该账号可用的模型 ID，点击「保存并授权连接」，在 Chrome 提示中允许连接 API 域名。每家服务独立保存配置，在主界面的「本次使用」中切换。

DeepSeek、Qwen、Kimi 提供可编辑的模型预设；OpenAI、Gemini、Claude 请从你的平台控制台复制模型 ID。模型预设不代表账号一定有权限，服务下线或变更时直接修改即可。Base URL 填写 API 根路径，不要再附加 /chat/completions 或 /messages。自定义服务需支持 OpenAI Chat Completions 协议，且使用 HTTPS。

Qwen 默认使用北京端点 https://dashscope.aliyuncs.com/compatible-mode/v1，Kimi 默认使用中国内地端点 https://api.moonshot.cn/v1。国际账号须改为与密钥匹配的服务地址。模型服务的网页聊天登录不能代替 API Key。

在论文页面点击「读取网页」，展开「查看正文」检查内容，再点「核心内容」「方法讲解」「创新与局限」，或输入任意问题。发送快捷键为 Ctrl+Enter（macOS 为 Command+Enter）。答案一次性显示，等待期间可点「停止」。

网页模式每次发送会刷新正文；URL 或正文变化后自动清空旧问答。换到新标签页时，请先点击一次工具栏中的扩展图标，让 Chrome 授予该页临时读取权限。仅打开侧边栏不一定授予新标签页权限。

若页面无法读取、需要解释单段文字、或使用浏览器 PDF 阅读器：展开正文区域，粘贴文字，点击「使用这段文字」。手动模式发送时固定使用这些文字；点击「读取网页」切回网页模式。

## 本版范围

- 面向期刊 HTML 网页，提取正文、标题、DOI、作者及小节标题；提取规则是通用启发式，网站结构不同可能混入导航或漏掉内容，请检查正文。
- 只读取当前已加载且可见的文字。不绕过付费墙，不自动展开全文，不读取跨域 iframe，不进行 PDF 解析、OCR、图片或公式截图分析。
- 网页只有摘要时，模型也只能解读摘要。建议先选择网站的 Full text / HTML 页面。
- 每次最多发送正文前 100,000 字符，超出会明确提示。另带元数据、系统提示与最近 4 轮成功问答。模型上下文限制较小时，可粘贴较短段落。
- 支持多轮追问和复制回答；对话只在当前侧边栏页面内存中保存，关闭或刷新可能丢失。
- 模型调用可能产生所选服务的 API 费用。停止请求不能保证服务商不计费。

## 数据与权限

没有自建服务器、分析埋点、远程脚本或依赖包。API Key 保存在 chrome.storage.local，仅可信扩展页面可读取，不同步到浏览器云端；它不是系统级加密保险库。可在设置中删除某家服务的密钥；卸载扩展会移除扩展存储。

只有点击发送或快捷问题时，阅读材料、页面 URL、问题和最近对话才直接发送给所选 API 地址。自定义地址也会接收你填写的密钥，请使用可信服务。点击「读取网页」只做本地提取。

tabs 用于识别当前标签页 URL，便于判断受限页面和按域名授权，不读取浏览历史。activeTab 与 scripting 用于点击扩展图标后读取当前网页；sidePanel 用于显示侧栏；storage 用于保存配置。安装时不请求所有网站的永久读取权限。optional_host_permissions 用于按域名授权：设置时授权 HTTPS API 地址；网页读取失败时，可点击“授权读取该网站”按钮，授予该 HTTP / HTTPS 网站读取权限。该授权持续有效，可在 Chrome 扩展详情中管理。

## 排错

- 读取失败：在目标网页再次点击扩展图标，或粘贴正文。
- HTTP 401 / 403：检查密钥、账号地区与 API 地址是否匹配。
- HTTP 404：检查 Base URL 和模型 ID。
- HTTP 400：检查模型是否支持对应对话协议，或减少输入长度。
- HTTP 429：检查额度与频率限制。
- 网络失败：检查本机是否能访问该 API 域名；修改地址后再次保存并授权。

## 验证范围与接口参考

交付时进行了 JavaScript 语法检查、模拟接口响应测试和本地浏览器页面测试；未使用真实 API Key，未验证各家账号的实际连通性或对所有期刊网站的适配。

官方接口参考：
- Chrome Side Panel: https://developer.chrome.com/docs/extensions/reference/api/sidePanel
- OpenAI: https://developers.openai.com/api/reference/resources/chat
- DeepSeek: https://api-docs.deepseek.com/
- Qwen: https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope
- Kimi: https://platform.kimi.com/docs/api/overview
- Gemini: https://ai.google.dev/gemini-api/docs/openai
- Claude: https://platform.claude.com/docs/en/api/messages/create


## 1.0.1 更新

修复网页缺少权限时无法从侧栏恢复的问题：增加按当前网站授权按钮；分别提示浏览器内部页、PDF、正文不足与脚本失败。增加 tabs 权限用于获取当前网页 URL。升级时 Chrome 如提示新的权限，请检查并允许；这不会授权所有网站的正文访问。

升级方法：将新版文件覆盖到原先加载的扩展文件夹（不要移动路径、不要卸载），在 chrome://extensions 点击此扩展的刷新按钮，关闭旧侧栏再重新打开。这样可保留原扩展的模型配置。

使用提供的 Nature 论文 https://www.nature.com/articles/s41586-026-10917-6 在独立 Chrome 中实际运行了正文提取，成功读出论文标题、DOI、Abstract、Main、Methods、Discussion 等内容。总正文约 81,000 字符，按本版限制保留前 80,000 字符并显示截断提示。浏览器网站授权的允许/拒绝流程使用模拟 Chrome API 验证；没有替代用户在真实侧栏中接受权限提示，也未调用真实模型 API。


## 1.0.2 更新

快捷按钮调整为：核心内容（研究问题、做法和结论）、方法讲解（关键步骤和原理）、创新与局限（新意、证据与边界）。按钮回答默认约 250–300 字以内。普通问答也默认简洁，简单问题一两句话即可；明确要求详细讲解、展开或完整翻译时按要求回答，字数不是硬性限制。


## 1.0.3 更新

通用回答要求和三个按钮的提示词已替换为确认后的版本：重点完整、解释清楚，不限制要点数量。一般以一千字以内为宜，但不是硬性上限，必要时增加篇幅。移除 1.0.2 中约 250–300 字及固定要点数量的要求。升级后建议清空已有对话，避免旧问题中的简短要求影响后续回答。


## 1.0.4 更新

网页读取及手动粘贴的正文上限均提高到 100,000 字符，超出仍显示截断提示。字符包含英文字母、空格、标点等，不是英文单词数，也不是 token 数；上下文还包含提示词、元数据、历史问答并需留出输出空间。原 Nature 测试提取的约 81,000 字符低于新上限。
